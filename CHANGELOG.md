# Changelog

このファイルは Spending-Log の変更履歴です。
バージョンは「メジャー.マイナー.軽微」の3点でナンバリングします。

## [0.6.2] - 修正
### Fixed
- インボイス制度の「登録番号」(T+数字13桁)をOCRが金額と誤認して読み取って
  しまう問題に対応。「登録番号」の行、およびT+数字の登録番号らしきトークンを
  金額候補から除外するように変更
- キーワード(「合計」等)に基づかない「テキスト中の数値の最大値」フォールバックを
  撤廃。実際のレシート写真での検証で、日付やレジ番号などのノイズを誤って
  金額として採用してしまうケースが確認されたため、キーワードで見つからない
  場合は数値を推測せず「読み取れませんでした。手入力してください」とするよう変更
- 実機のカメラ写真(数千px四方)をそのままOCRすると数十秒以上かかっていた
  問題を修正。OCR前に長辺1600pxまで縮小するように変更
### Known limitations
- 実際のレシート写真(背景に机や木目などが写り込む、感熱紙の印字が薄い等)では、
  背景のノイズがOCRに悪影響を与え、「合計」自体を正しく読み取れず金額欄が
  空のままになる、まれに行内の数字が一部だけ読み取られる、といったケースが
  依然としてある。詳細は `spec/receipt-ocr.md` を参照

## [0.6.1] - 修正
### Fixed
- レシートOCRの金額抽出精度が低い問題を修正。テキストの改行(行)単位でのキーワード
  一致に頼っていたため、ラベルと金額が離れたレイアウトでずれることがあった
- OCRの単語ごとの座標(bbox)情報を使い、「合計」の文言と**同じ行(Y座標)**にある
  金額を最優先で抽出する方式(`extractAmountFromWords`)に変更
- 単純に「一番大きい数字」を採用するのではなく、「合計」の横に書かれた金額を
  最優先するロジックに変更(座標データが取得できない場合のみ、従来の行ベース→
  最大値のロジックにフォールバック)

## [0.6.0] - 実装
### Added
- レシート画像を読み込んで金額を自動読取する機能を追加(`spec/receipt-ocr.md` の仕様に基づく実装)
  - 入力/編集モーダルに「📷 レシート読込(金額を自動入力)」ボタンを追加
  - タップすると端末のカメラ起動/写真選択(`<input type="file" accept="image/*" capture="environment">`)
  - Tesseract.js(CDN経由、日本語データ)をブラウザ内(WASM)で実行しOCR。画像は
    端末外に送信しない
  - 「合計」等のキーワード優先で金額を抽出し、金額欄に自動入力(自動記録はせず、
    必ずユーザーが確認・修正できる状態のみ)
  - 選択した画像のサムネイルと読取ステータスをモーダル内に表示
  - オフライン/OCR失敗時は「手入力してください」に自動フォールバック
  - レシート画像自体は記録データとして保存しない(OCR用途のみ、モーダルを
    閉じる・記録すると破棄)

## [0.5.7] - 修正
### Fixed
- PWAを全画面(スタンドアロン)で使用した場合にホームインジケーターとまだ干渉する
  問題に対応。浮かせる距離を10px→24pxに拡大し、干渉が起きにくいよう余裕を持たせた
- スワイプゾーン自体の高さも40px→56pxに拡大し、タップ・スワイプしやすくした
- `body` の `padding-bottom` をゾーンの新しいサイズ・位置に合わせて再調整

## [0.5.6] - 修正
### Fixed
- 下部スワイプゾーンが画面最下端(`bottom:0`)に張り付いていたため、iOSの
  ホームインジケーター(画面下端からのスワイプでホームに戻るジェスチャー領域)と
  干渉する問題を修正
- ゾーンを画面最下端から10px浮かせて配置し、高さも56px→40pxに縮小
- 左右にも12pxの余白を設け、カード状の見た目に変更(`border-radius`付き)
- `body` の `padding-bottom` をゾーンの新しいサイズ・位置に合わせて調整

## [0.5.5] - 追加
### Added
- 下部スワイプゾーンに、遷移できる方向を示す矢印記号(‹ ›)を追加
- 現在のタブ位置に応じて、遷移できない側(タブ1での‹、タブ3での›)は
  自動的にグレーアウトするように対応

## [0.5.4] - 変更
### Changed
- タブ切替のスワイプ判定方式を刷新: 画面下部に専用のスワイプゾーン(`#swipeZone`、
  高さ56px、つまみ状のインジケーター付き)を新設し、そこでの左右スワイプで
  タブを切替える方式に変更
- カレンダー内(月切替)とスワイプ領域が完全に分離されたため、両者の競合を考慮する
  必要がなくなり、判定ロジックをシンプル化(開始位置による分岐ロジックを撤廃)
- 下部ゾーンはどのタブでも常に同じ場所・同じ挙動で動作するため、全タブで共通の
  スワイプ操作感になった
- `body` の下部余白をスワイプゾーン分(56px + セーフエリア)確保するよう調整

## [0.5.3] - 改善
### Changed
- カレンダー内で始まったスワイプによるタブ切替が「130px以上」という大きな距離を
  要求していたため反応が鈍く感じられた問題を改善
- 月切替とタブ切替の区別を「スワイプ距離」ではなく「スワイプの開始位置」に変更:
  画面端付近(EDGE_ZONE=32px以内)から始まったスワイプはタブ切替、
  それ以外(グリッド中央側)は月切替とする方式に変更
- これにより両方の操作のしきい値を50px/60pxまで下げられ、体感の反応速度が向上

## [0.5.2] - 修正
### Fixed
- Service Worker(`sw.js`)のキャッシュ戦略が stale-while-revalidate になっており、
  `CACHE_NAME` もv0.1.0から一度も更新されていなかったため、pushしても実機では
  1回のリロードでは新しいコードが反映されない不具合を修正
- `CACHE_NAME` を `spending-log-cache-v2` に更新(古いキャッシュを破棄)
- fetch戦略を network-first(通信できる場合は常に最新を取得し、オフライン時のみ
  キャッシュにフォールバック)に変更し、以後のアップデートが即座に反映されるように対応

## [0.5.1] - 修正
### Fixed
- タブ1(カレンダー)では `calGrid` が画面のほぼ全域を占めており、そこで始まるスワイプが
  常に月切替専用として処理されタブ切替が一切発火しない不具合を修正
- カレンダー内で始まったスワイプは、通常距離(130px未満)なら従来通り月切替、
  130px以上の大きめのスワイプならタブ切替とみなすよう分岐し、両方の操作を両立

## [0.5.0] - 実装
### Added
- モーダル(入力フォーム・週/月グラフの内訳画面など)が表示されていないときに限り、画面全体のスワイプでタブ(カレンダー/週月グラフ/月履歴)を切り替えられるように対応
- カレンダー内でのスワイプ(月切替)とは判定を分離し、互いに干渉しないようにした

## [0.4.0] - 実装
### Added
- PWA化: `manifest.json` を追加(ホーム画面追加時のアプリ名・アイコン・スタンドアロン表示に対応)
- `sw.js`(service worker)を追加。初回アクセス時に主要ファイルをキャッシュし、オフラインでも起動できるように対応
- アイコン一式(`docs/icons/`)を追加: icon-512.png / icon-192.png / apple-touch-icon.png / favicon-32.png
  - デザイン: 白背景、銀行の地図記号(建物モチーフ)をベースに中央の柱を光熱費カラー(緑)でアレンジ、下部に「sp-log」表示
- `index.html` の `<head>` に manifest・アイコン・apple-mobile-web-app関連のmetaタグを追加

## [0.3.0] - 実装
### Changed
- 週間円グラフ・月間円グラフをタップした際の挙動を、カード内での差し替えから**別画面(モーダル)表示**に変更
- 別画面では、大項目(光熱費/生活費/雑費)ごとに小項目別の円グラフを縦に並べて表示

## [0.2.0] - 実装
### Fixed
- カレンダー日付欄をタップした際、既存レコードがあると常に編集モードで開いてしまい、同日・同大項目で複数記録を追加すると上書きされる不具合を修正
### Changed
- カレンダー日付欄の表示を「個別レコードごと」から「大項目ごとの小計」表示に変更
- 日付欄のタップ挙動を分離: 日付の数字部分 → 常に新規追加 / 大項目の小計ラベル → その日・その大項目のレコード一覧(編集・追加)を表示する別画面

## [0.1.0] - 実装
### Added
- 初版実装(単一HTMLファイル、localStorage使用)
- タブ1: カレンダー(月曜始まり、スワイプ+ボタンで月切替)、基本入力フォーム(大項目→小項目→金額→メモ)、記録の追加・編集・削除(削除は確認ダイアログあり)
- タブ2: 週間円グラフ(3列×2行グリッド、5週丸めロジック)、月間円グラフ
- タブ3: 直近3ヶ月の月間積み上げ棒グラフ(縦軸自動スケール)、それ以前の履歴はスクロール一覧
- Safari対策(input/textareaのfont-sizeを16px以上に固定、user-scalable=no)
